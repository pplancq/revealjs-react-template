import { Reveal } from '@Front/components/Reveal/Reveal';

export const App = () => {
  return (
    <Reveal>
      <div className="slides">
        <section>
          <h1>Reveal.js React Template</h1>
        </section>
      </div>
    </Reveal>
  );
};
